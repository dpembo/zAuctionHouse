org.gradle.java.home=/usr/lib/jvm/temurin-21-jdk-amd64
#java.version=25
plugin.version=1.0.0

# Hooks that require external libraries not distributed with the project.
# Set to true to include them in the build (requires the JAR in Hooks/<name>/libs/).
hooks.zelauction=true
hooks.donutauction=true
hooks.crazyauctions=true
